version: "3.9"
services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: pulsedb
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: secret
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
